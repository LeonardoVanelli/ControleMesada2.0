import linearGradient from 'react-native-linear-gradient';
import styled from 'styled-components';

export default styled(linearGradient).attrs({
  colors: ['#0E285B', 'rgba(16, 92, 87, 0.85)'],
})`
  flex: 1;
`;
